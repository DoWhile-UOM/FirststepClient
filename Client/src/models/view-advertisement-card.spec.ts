import { ViewAdvertisementCard } from './view-advertisement-card';

describe('ViewAdvertisementCard', () => {
  it('should create an instance', () => {
    expect(new ViewAdvertisementCard()).toBeTruthy();
  });
});
